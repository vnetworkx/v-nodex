# Consensus

Consensus in v-nodex is a node-side acceptance policy over kernel-produced records.

It handles:
- canonical head selection
- peer agreement metadata
- conflict detection
- finality markers

It does not execute protocol math.
