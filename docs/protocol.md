# Protocol

The node accepts kernel-approved records and stores them immutably.

Protocol truth remains in Rust `v-kernelx`.

Go only:
- forwards requests
- stores records
- syncs peers
- rebuilds indexes
- exposes APIs
