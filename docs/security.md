# Security

- store identity keys outside source control
- disable debug routes in production
- keep protocol signing in Rust
- enforce request size limits
- rate limit public APIs
- verify peer data before storing it
